{
  "name": "redy",
  "version": "0.0.0",
  "main": "dist/redy.umd.js",
  "files": [
    "dist"
  ],
  "author": "ryym <ryym.64@gmail.com>",
  "repository": {
    "type": "git",
    "url": "https://github.com/ryym/redy"
  },
  "license": "MIT",
  "engines": {
    "node": ">=6.0.0"
  },
  "scripts": {
    "lint": "tslint  --project tsconfig.json 'src/**/*.ts' 'test/**/*.ts'",
    "prebuild": "rm -rf dist",
    "build": "tsc",
    "build:watch": "tsc --watch",
    "start": "tsc -w",
    "test": "jest"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "{src,test}/**/*.ts": [
      "prettier --write",
      "git add"
    ]
  },
  "dependencies": {
    "redux": "^4.0.1"
  },
  "devDependencies": {
    "@types/jest": "^23.3.10",
    "husky": "^1.2.1",
    "jest": "^23.6.0",
    "lint-staged": "^8.1.0",
    "prettier": "^1.15.3",
    "ts-jest": "^23.10.5",
    "tslint": "^5.12.0",
    "typescript": "^3.2.2"
  }
}
